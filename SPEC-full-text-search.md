# Spec: full-text-search

## Objective

对活动文档的原文件名和已提取正文建立完全离线索引，提供中文关键词搜索、相关度排序、高亮及分类/格式筛选。

## Decisions

- 优先使用 SQLite FTS5 trigram 索引以支持中文连续文本检索。
- 目标SQLite缺少FTS5或查询不足三个字符时，使用参数化本地回退查询。
- 文件名匹配权重高于正文；回收站文档不出现在结果中。
- 索引可从 documents 表完整重建，不是唯一数据来源。
- 第一版不提供 AND/OR 等高级语法，将输入视为普通文字。

## Success Criteria

- 中文正文与文件名均可命中并高亮。
- 可按唯一分类和 `.doc/.docx` 格式筛选。
- 已删除、未解析或不满足筛选条件的文档不会返回。
- 索引损坏后可重建，单文档失败不终止重建。

