# Spec: classification

## Objective

提供最多三级、每份文档唯一归属的虚拟分类，并使用完全离线的关键词与人工样本生成可解释推荐。

## Decisions

- 系统固定保留“未分类”状态，不将其作为可删除分类记录。
- 非空分类删除前必须迁移文档到另一分类或未分类。
- 分类包含名称、说明和关键词；推荐同时参考已人工分类文档的正文词项。
- 可信度分低、中、高；默认仅推荐。自动归类结果标记为待复核。
- 不承诺固定准确率，推荐必须返回命中的分类依据。

## Success Criteria

- 无法创建第四级分类或造成循环。
- 文档只能有一个分类。
- 非空分类不会连带删除文档。
- 关键词和样本均能影响推荐结果；无依据时不推荐。

