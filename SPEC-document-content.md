# Spec: document-content

## Objective

从资料库中的 `.docx/.doc` 提取可搜索正文，提供阅读型预览数据和来源引用，并隔离单文档失败。

## Decisions

- `.docx` 使用离线内置解析，按标题、段落、表格单元格顺序产生纯文本。
- `.doc` 通过可替换本地解析器链处理；找不到兼容解析器时标记失败，不影响其他文档。
- 不执行文档宏、外链、嵌入程序或网络资源。
- 预览不承诺分页、页眉页脚、批注、复杂公式和原始版式一致。
- 引用格式为所选正文加文档名及段落位置，不生成不可靠页码。
- WPS只负责打开资料库原件；程序检测保存变化后重新解析。

## Success Criteria

- 普通中文 `.docx` 段落和表格文字可提取。
- 加密、损坏或不支持的文档产生稳定错误码并更新解析状态。
- 引用支持“仅正文”和“正文＋来源”两种形式。
- 文档内容和文件名不会进入诊断日志。

