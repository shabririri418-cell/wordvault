# Spec: offline-delivery

## Objective

在兼容的银河麒麟 AArch64 构建机上生成无需联网安装和运行的双击安装包，并支持数据库升级前备份及失败诊断。

## Build Contract

- 构建机必须为 AArch64；脚本拒绝在其他架构生成客户包。
- 所有 Python wheel 预先放入 `vendor/wheels`，构建阶段使用 `--no-index`。
- PyInstaller产物安装到 `/opt/wordvault`，桌面入口安装到系统应用菜单。
- 客户设备不安装Python包、不拉取系统仓库、不访问网络。
- 客户解压交付包后只需双击“双击安装文澜资料库”，不需要打开终端或输入命令。
- 图形安装器自动校验安装包、检查架构与依赖，安装成功后自动启动应用。
- 安装失败时在交付目录生成不含文档内容的“安装结果.txt”。
- 发布前在目标机执行导入、搜索、WPS打开、回收站、诊断导出验收。

## Upgrade Safety

- 发现旧数据库版本时，先使用SQLite在线备份API生成版本化备份。
- 模式迁移在事务中执行；失败时不得删除原资料文件。
- 软件不提供完整资料库备份，升级备份只保护数据库结构与分类数据。

## Success Criteria

- AArch64构建脚本仅从离线缓存安装依赖并生成`.deb`。
- 安装后普通用户可启动，运行过程中无网络依赖。
- 数据库升级测试证明备份保持旧版本且新库迁移成功。
- 脱敏诊断包通过负向泄密测试。
