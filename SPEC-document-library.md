# Spec: document-library

## Objective

提供由程序独立管理的文档资料库。导入始终复制源文件，资料库内部使用 UUID 文件名，分类不映射为物理目录。

## Behaviors

- 接受单个 `.doc/.docx` 或递归文件夹导入，保留原文件及本地相对来源记录。
- 内容相同或原文件名相同时返回冲突，必须选择覆盖、保留为新文档或取消。
- 覆盖不保存旧版本；复制完全成功后才更新数据库。
- 删除进入内部回收站；恢复回原记录；清空后永久删除。
- 导出只复制，恢复原文件名；同名时支持覆盖、自动重命名或跳过。
- 启动扫描资料库实际文件与数据库记录的差异并返回可修复问题。

## Data and Privacy

- 数据库保存原文件名、来源相对路径、散列和状态，但这些字段永不进入诊断日志。
- 文件操作只允许发生在已解析的资料库 `documents/`、`trash/` 或用户明确选择的导出目录。
- 单文件失败不得回滚已经成功导入的其他文件。

## Testing

- 使用临时目录和真实 SQLite 测试复制、冲突、覆盖、导出、回收站和一致性检查。
- 覆盖失败时验证原资料和数据库记录保持不变。
- 中文、空格和特殊字符文件名必须通过测试。

## Success Criteria

- 导入不会修改或删除源文件。
- 三种冲突决策结果确定且可恢复失败。
- 回收站与导出闭环通过集成测试。
- 资料库外部变动可被识别，不静默丢失记录。

